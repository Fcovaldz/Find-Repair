import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    imagen: {
        resizeMode: "contain",
        width: "100%",
        height: 150,
        marginTop: 20,
    },
    contenido: {
        marginBottom: 20
    }
});